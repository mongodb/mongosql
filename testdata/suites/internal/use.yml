testcases:

  - db: use_test
    description: use
    expected_names: [b]
    expected_results:
      - ['2']
    expected_types: [varchar]
    sql: use use_test2
    unordered: true
    verify: select b from test2
