testcases:

  - expected_names: [none_strC_nk]
    expected_results:
      - [sixteen]
      - [ten]
      - [three]
      - [twelve]
      - [two]
    expected_types: [varchar]
    sql: SELECT `str2` AS `none_strC_nk` FROM Calcs WHERE (NOT ((`str2` IS NULL) OR ((`str2` >= 'eight') AND (`str2` <= 'six')))) GROUP BY `str2` ORDER BY 1
