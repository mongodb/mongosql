testcases:

  - expected_names: [none_Call_Center_Region_nk]
    expected_results:
      - [CENTRAL]
      - [EAST]
      - [WEST]
    expected_types: [varchar]
    sql: SELECT `Call Center Region` AS `none_Call_Center_Region_nk` FROM Staples GROUP BY `Call Center Region` ORDER BY 1 ASC
    unordered: true
