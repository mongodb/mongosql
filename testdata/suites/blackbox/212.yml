testcases:

  - expected_names: [none_Prod_TypeB_nk]
    expected_results:
      - [FURNITURE]
      - [OFFICE SUPPLIES]
      - [TECHNOLOGY]
    expected_types: [varchar]
    sql: SELECT `Prod Type1` AS `none_Prod_TypeB_nk` FROM Staples GROUP BY `Prod Type1` ORDER BY 1 ASC
    unordered: true
