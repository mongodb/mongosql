testcases:

  - expected_names: [qr_Date_ok]
    expected_results: []
    expected_types: [varchar]
    sql: SELECT QUARTER(`Date`) AS `qr_Date_ok` FROM Starbucks WHERE (QUARTER(`Date`) IS NULL) LIMIT 1
