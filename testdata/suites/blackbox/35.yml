
testcases:
- sql: "SELECT COUNT(1) AS `cnt_Number_of_Records_ok`,   `Employee Name` AS `none_Employee_Name_nk` FROM Staples WHERE (`Employee Name` IN ('Sandra Davis', 'Sandra Ríos Balderrama')) GROUP BY `Employee Name` ORDER BY 1, 2"
  min_server_version: "3.4"
  expected_names: ["cnt_Number_of_Records_ok", "none_Employee_Name_nk"]
  expected_types: [int, varchar]
  expected:
  - [8, "Sandra Davis"]
  - [95, "Sandra Ríos Balderrama"]

