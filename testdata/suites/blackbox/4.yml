testcases:

  - expected_names: [none_Market_nk]
    expected_results: []
    expected_types: [varchar]
    sql: SELECT `Market` AS `none_Market_nk` FROM Starbucks WHERE (`Market` IS NULL) ORDER BY 1 LIMIT 1
    unordered: true
