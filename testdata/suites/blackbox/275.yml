testcases:

  - expected_names: [none_Market_nk]
    expected_results:
      - [Central]
      - [East]
      - [South]
      - [West]
    expected_types: [varchar]
    sql: SELECT `Market` AS `none_Market_nk` FROM Starbucks GROUP BY `Market` ORDER BY 1 ASC
