
testcases:
- sql: "SELECT MIN(1) AS `min_Number_of_Records_ok`, `Market` AS `none_Market_nk`, `Product Type` AS `none_Product_Type_nk`, SUM(CONVERT(`Budget Profit`, SQL_BIGINT)) AS `sum_Budget_Profit_ok` FROM Starbucks WHERE (`Market` = 'Central') GROUP BY `Market`, `Product Type` ORDER BY 1, 2, 3, 4"
  expected_names: ["min_Number_of_Records_ok", "none_Market_nk", "none_Product_Type_nk", "sum_Budget_Profit_ok"]
  expected_types: [int64, varchar, varchar, int64]
  expected:
  - [1, "Central", "Coffee", 24220]
  - [1, "Central", "Espresso", 28520]
  - [1, "Central", "Herbal Tea", 22740]
  - [1, "Central", "Tea", 17100]

