testcases:

  - expected_names: [none_Type_nk]
    expected_results: []
    expected_types: [varchar]
    sql: SELECT `Type` AS `none_Type_nk` FROM Starbucks WHERE (`Type` IS NULL) ORDER BY 1 LIMIT 1
