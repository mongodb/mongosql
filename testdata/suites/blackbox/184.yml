
testcases:
- sql: "SELECT `MONTH` AS `none_MONTH_nk`, SUM(CONVERT(`AUTOTHEFT`, SQL_BIGINT)) AS `sum_AUTOTHEFT_ok` FROM SeattleCrime GROUP BY `MONTH` HAVING ((SUM(CONVERT(`ARSON`, SQL_BIGINT)) >= 40) AND (SUM(CONVERT(`ARSON`, SQL_BIGINT)) <= 55)) ORDER BY 1, 2"
  expected_names: ["none_MONTH_nk", "sum_AUTOTHEFT_ok"]
  expected_types: [varchar, int64]
  expected:
  - ["April", 814]
  - ["August", 798]
  - ["July", 774]
  - ["June", 602]
  - ["May", 597]
  - ["November", 883]
  - ["October", 904]
  - ["September", 777]

