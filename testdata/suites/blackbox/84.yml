
testcases:
- sql: "SELECT SUM(CONVERT(`Margin`, SQL_BIGINT)) AS `sum_Margin_ok` FROM Starbucks HAVING (COUNT(1) > 0) ORDER BY 1"
  expected_names: ["sum_Margin_ok"]
  expected_types: [int64]
  expected:
  - [443038]

