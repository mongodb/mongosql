testcases:

  - expected_names: [none_Product_In_Stock_nk]
    expected_results:
      - ['NO']
      - ['YES']
    expected_types: [varchar]
    sql: SELECT `Product In Stock` AS `none_Product_In_Stock_nk` FROM Staples GROUP BY `Product In Stock` ORDER BY 1 ASC
