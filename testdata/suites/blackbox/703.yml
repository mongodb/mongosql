testcases:

  - expected_names: [none_strA_nk, none_z_const_str_nk]
    expected_results:
      - [FURNITURE, CONST]
      - [OFFICE SUPPLIES, CONST]
      - [TECHNOLOGY, CONST]
    expected_types: [varchar, varchar]
    sql: SELECT `str0` AS `none_strA_nk`,   'CONST' AS `none_z_const_str_nk` FROM Calcs GROUP BY `str0` ORDER BY 1, 2
    unordered: true
