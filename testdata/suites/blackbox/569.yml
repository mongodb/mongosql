
testcases:
- sql: "SELECT `MONTH` AS `none_MONTH_nk`, SUM(CONVERT(`AUTOTHEFT`, SQL_BIGINT)) AS `sum_AUTOTHEFT_ok` FROM SeattleCrime WHERE ((`ARSON` >= 4) AND (`ARSON` <= 16)) GROUP BY `MONTH` HAVING ((SUM(CONVERT(`ARSON`, SQL_BIGINT)) >= 40) AND (SUM(CONVERT(`ARSON`, SQL_BIGINT)) <= 55)) ORDER BY 1, 2"
  expected_names: ["none_MONTH_nk", "sum_AUTOTHEFT_ok"]
  expected_types: [varchar, int]
  expected:
  - ["November", 58]

