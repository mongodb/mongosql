testcases:

  - expected_names: [none_App_Employ_Status_nk]
    expected_results:
      - [EMPLOYED]
      - [UNEMPLOYED]
    expected_types: [varchar]
    sql: SELECT `App_Employ_Status` AS `none_App_Employ_Status_nk` FROM Loan GROUP BY `App_Employ_Status` ORDER BY 1 ASC
    unordered: true
