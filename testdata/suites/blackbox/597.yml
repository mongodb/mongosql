testcases:

  - expected_names: [none_Product_Container_nk]
    expected_results:
      - [JUMBO BOX]
      - [JUMBO DRUM]
      - [LARGE BOX]
      - [MEDIUM BOX]
      - [SMALL BOX]
      - [SMALL PACK]
      - [WRAP BAG]
    expected_types: [varchar]
    sql: SELECT `Product Container` AS `none_Product_Container_nk` FROM Staples GROUP BY `Product Container` ORDER BY 1 ASC
    unordered: true
