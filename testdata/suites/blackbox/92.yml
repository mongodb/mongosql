testcases:

  - expected_names: [sum_Profit_ok]
    expected_results:
      - [259527.27268291984]
    expected_types: [float]
    sql: SELECT SUM(`Profit`) AS `sum_Profit_ok` FROM Starbucks HAVING (COUNT(1) > 0) ORDER BY 1
