
testcases:
- sql: "SELECT SUM(CONVERT(`Margin`, SQL_BIGINT)) AS `sum_Margin_ok`, SUM(`Profit`) AS `sum_Profit_ok`, SUM(`Sales`) AS `sum_Sales_ok`, YEAR(`Date`) AS `yr_Date_ok` FROM Starbucks GROUP BY YEAR(`Date`) ORDER BY 1, 2, 3, 4"
  expected_names: ["sum_Margin_ok", "sum_Profit_ok", "sum_Sales_ok", "yr_Date_ok"]
  expected_types: [int, float, float, int]
  expected:
  - [221519, 105826, 401159, 1998]
  - [221519, 153701.27268291984, 418668.1002277897, 1999]

