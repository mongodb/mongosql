
testcases:
- sql: "SELECT `str2` AS `none_strC_nk`, (CASE WHEN (`str2` IS NULL) THEN NULL WHEN (`str2` IN ('eight', 'eleven')) THEN 'eight' WHEN (`str2` IN ('sixteen', 'ten')) THEN 'sixteen' WHEN (`str2` IN ('three', 'twelve')) THEN 'three' ELSE 'two' END) AS `strC__group__B`, SUM(CONVERT(1, SQL_BIGINT)) AS `sum_Number_of_Records_ok` FROM Calcs GROUP BY `str2`, (CASE WHEN (`str2` IS NULL) THEN NULL WHEN (`str2` IN ('eight', 'eleven')) THEN 'eight' WHEN (`str2` IN ('sixteen', 'ten')) THEN 'sixteen' WHEN (`str2` IN ('three', 'twelve')) THEN 'three' ELSE 'two' END) ORDER BY 1, 2, 3"
  expected_names: ["none_strC_nk", "strC__group__B", "sum_Number_of_Records_ok"]
  expected_types: [varchar, varchar, int64]
  expected:
  - [~, ~, 4]
  - ["eight", "eight", 1]
  - ["eleven", "eight", 1]
  - ["fifteen", "two", 1]
  - ["five", "two", 1]
  - ["fourteen", "two", 1]
  - ["nine", "two", 1]
  - ["one", "two", 1]
  - ["six", "two", 1]
  - ["sixteen", "sixteen", 1]
  - ["ten", "sixteen", 1]
  - ["three", "three", 1]
  - ["twelve", "three", 1]
  - ["two", "two", 1]

