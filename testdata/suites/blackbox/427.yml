testcases:

  - expected_names: [none_BBAEFH_nk]
    expected_results:
      - ['*Anonymous*']
      - [CORPORATE]
      - [HOME OFFICE]
      - [SMALL BUSINESS]
    expected_types: [varchar]
    min_server_version: '3.4'
    sql: SELECT (CASE `Customer Segment` WHEN 'CONSUMER' THEN '*Anonymous*' ELSE `Customer Segment` END) AS `none_BBAEFH_nk` FROM Staples GROUP BY (CASE `Customer Segment` WHEN 'CONSUMER' THEN '*Anonymous*' ELSE `Customer Segment` END) ORDER BY 1
