testcases:

  - expected_names: [none_Prod_TypeE_nk]
    expected_results: []
    expected_types: [varchar]
    sql: SELECT `Prod Type4` AS `none_Prod_TypeE_nk` FROM Staples WHERE (`Prod Type4` IS NULL) ORDER BY 1 LIMIT 1
    unordered: true
