testcases:

  - expected_names: [none_App_College_nk]
    expected_results:
      - [COLLEGE GRAD]
      - [HIGH SCHOOL GRAD]
    expected_types: [varchar]
    sql: SELECT `App_College` AS `none_App_College_nk` FROM Loan GROUP BY `App_College` ORDER BY 1 ASC
