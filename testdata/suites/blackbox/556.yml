
testcases:
- sql: "SELECT SUM(CONVERT(`Budget COGS`, SQL_BIGINT)) AS `sum_Budget_COGS_ok`, SUM(`Sales`) AS `sum_Sales_ok`, SUM(CONVERT((`Budget COGS` - `Budget Margin`), SQL_BIGINT)) AS `sum_Stupid_Calc_ok`, SUM(`Total Expenses`) AS `sum_Total_Expenses_ok` FROM Starbucks HAVING (COUNT(1) > 0) ORDER BY 1, 2, 3, 4"
  expected_names: ["sum_Budget_COGS_ok", "sum_Sales_ok", "sum_Stupid_Calc_ok", "sum_Total_Expenses_ok"]
  expected_types: [int64, float64, int64, float64]
  expected:
  - [317880, 819827.1002277897, -110400, 229666.11]

