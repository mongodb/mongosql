
testcases:
- sql: "SELECT COUNT(`TRACT`) AS `TEMP_Avg_Rape_Per_Tract__DGDBIHAADB__A_`, SUM(CONVERT(`RAPE`, SQL_BIGINT)) AS `TEMP_Avg_Rape_Per_Tract__HDEJFCFIH__A_`, `PRECINCT` AS `none_PRECINCT_nk` FROM SeattleCrime GROUP BY `PRECINCT` HAVING ((CASE WHEN COUNT(`TRACT`) = 0 THEN NULL ELSE CONVERT(SUM(CONVERT(`RAPE`, SQL_BIGINT)), SQL_DOUBLE) / COUNT(`TRACT`) END) < 0.12) ORDER BY 1, 2, 3"
  expected_names: ["TEMP_Avg_Rape_Per_Tract__DGDBIHAADB__A_", "TEMP_Avg_Rape_Per_Tract__HDEJFCFIH__A_", "none_PRECINCT_nk"]
  expected_types: [int64, int64, varchar]
  expected:
  - [12, 0, "EAST DISTRICT"]
  - [12, 0, "N/A"]
  - [12, 0, "WEST DISTRICT"]
  - [216, 14, "SOUTHWEST PRECINCT"]
  - [228, 25, "EAST PRECINCT"]
  - [672, 56, "NORTH PRECINCT"]

