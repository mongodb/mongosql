testcases:

  - expected_names: [cnt_Key_ok]
    expected_results:
      - [16]
    expected_types: [int]
    sql: SELECT COUNT(`Key`) AS `cnt_Key_ok` FROM NumericBins HAVING (COUNT(1) > 0) ORDER BY 1
    unordered: true
