testcases:

  - expected_names: [yr_Date_ok]
    expected_results:
      - [1998]
      - [1999]
    expected_types: [int]
    sql: SELECT YEAR(`Date`) AS `yr_Date_ok` FROM Starbucks GROUP BY YEAR(`Date`) ORDER BY 1
