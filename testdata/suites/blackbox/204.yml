testcases:

  - expected_names: [none_Customer_Segment_nk]
    expected_results:
      - [CONSUMER]
      - [CORPORATE]
      - [HOME OFFICE]
      - [SMALL BUSINESS]
    expected_types: [varchar]
    sql: SELECT `Customer Segment` AS `none_Customer_Segment_nk` FROM Staples GROUP BY `Customer Segment` ORDER BY 1 ASC
