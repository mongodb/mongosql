testcases:

  - expected_names: [none_PRECINCT_nk]
    expected_results: []
    expected_types: [varchar]
    sql: SELECT `PRECINCT` AS `none_PRECINCT_nk` FROM SeattleCrime WHERE (`PRECINCT` IS NULL) ORDER BY 1 LIMIT 1
    unordered: true
