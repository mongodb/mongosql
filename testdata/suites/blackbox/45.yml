testcases:

  - expected_names: [none_Customer_Name_nk]
    expected_results: []
    expected_types: [varchar]
    sql: SELECT `Customer Name` AS `none_Customer_Name_nk` FROM Staples WHERE (`Customer Name` IS NULL) ORDER BY 1 LIMIT 1
    unordered: true
