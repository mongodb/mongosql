benchmarks:

  - {name: lpad, query: 'select lpad(s, 20, ''#'') from strings', type: query}

  - {name: lpad_long, query: 'select lpad(s, 2000, ''#'') from strings', type: query}

  - {name: lpad_many_column_args, query: 'select lpad(s, 20, s) from strings', type: query}

  - {name: lpad_long_many_column_args, query: 'select lpad(s, 2000, s) from strings', type: query}

  - {name: lpad_long_nested, query: 'select lpad(s, 20000, lpad(s, 20, s)) from strings', type: query}

  - {name: count_million, query: select count(*) from count;, type: query}

  - {name: count_hundred_thousand, query: select count(*) from count;, type: query}

  - {name: count_thousand, query: select count(*) from count;, type: query}

  - {name: select_scalar_noconflict, query: select * from scalar_noconflict, type: query}

  - {name: select_scalar_conflict, query: select * from scalar_conflict, type: query}

  - {name: select_scalar_noconflict_where, query: select * from scalar_noconflict where a = "hello", type: query}

  - {name: select_scalar_conflict_where, query: select * from scalar_conflict where a = "hello", type: query}

  - {name: select_nested_object_noconflict, query: select * from nested_object_noconflict, type: query}

  - {name: select_nested_object_conflict, query: select * from nested_object_conflict, type: query}

  - {name: select_nested_object_noconflict_where, query: select * from nested_object_noconflict where  `a.b.c` = 42, type: query}

  - {name: select_nested_object_conflict_where, query: select * from nested_object_conflict where `a.b.c` = 42, type: query}

  - {name: select_nested_object_array_noconflict, query: select * from nested_object_array_noconflict, type: query}

  - {name: select_nested_object_array_conflict, query: select * from nested_object_array_conflict, type: query}

  - {name: select_nested_object_array_conflict_a, query: select * from nested_object_array_conflict_a, type: query}

  - {name: select_nested_object_array_conflict_a_b, query: select * from nested_object_array_conflict_a_b, type: query}

  - {name: conversions_many, query: 'select convert(non_numeric_string, datetime), convert(bool, decimal), convert(''double'', signed), convert(''int'', char), convert(numeric_string, decimal) from conversions;', type: query}

  - {name: conversions_none, query: 'select non_numeric_string, bool, ''double'', ''int'', numeric_string from conversions;', type: query}

  - {name: complex_predicate_expr, query: 'select * from items where rpad(lpad(a, 200, ''abc''), 500, ''def'') = lpad(rpad(b, 200, ''def''), 500, ''abc'')', type: query}
