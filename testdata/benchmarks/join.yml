benchmarks:

  - {name: cross_join, query: select * from foo join bar limit 1000;, type: query}

  - {name: inner_join_equijoin, query: select * from foo join bar on foo._id = bar._id limit 1000;, type: query}

  - {name: inner_join_non_equijoin, query: select * from foo join bar on foo._id < bar._id limit 1000;, type: query}

  - {name: left_join_equijoin, query: select * from foo left join bar on foo._id = bar._id limit 1000;, type: query}

  - {name: left_join_non_equijoin, query: select * from foo left join bar on foo._id < bar._id limit 1000;, type: query}

  - {name: right_join_equijoin, query: select * from foo right join bar on foo._id = bar._id limit 1000;, type: query}

  - {name: right_join_non_equijoin, query: select * from foo right join bar on foo._id < bar._id limit 1000;, type: query}

  - {name: parent_child_on_fk, query: select * from complex_docs_d join complex_docs on complex_docs_d._id = complex_docs._id, type: query}

  - {name: parent_child_not_on_fk, query: select * from complex_docs_d join complex_docs on complex_docs_d.`d.b` = complex_docs.c, type: query}

  - {name: grandchild_child_on_fk, query: select * from complex_docs_d_a join complex_docs_d on complex_docs_d_a._id = complex_docs_d._id AND complex_docs_d_a.d_idx = complex_docs_d.d_idx, type: query}

  - {name: grandchild_child_not_on_fk, query: select * from complex_docs_d_a join complex_docs_d on complex_docs_d_a.`d.a.a` = complex_docs_d.`d.b`, type: query}

  - {name: grandchild_child_not_on_fk_covered_query, query: select complex_docs_d_a.`d.a.a` from complex_docs_d_a join complex_docs_d on complex_docs_d_a.`d.a.a` = complex_docs_d.`d.b`, type: query}
