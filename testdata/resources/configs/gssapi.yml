mongodb:
  net:
    auth: {mechanism: GSSAPI, password: powerbook17, username: drivers@LDAPTEST.10GEN.CC}
    uri: mongodb://ldaptest.10gen.cc:27017
net:
  bindIp: 127.0.0.1
  unixDomainSocket: {enabled: false, filePermissions: '0600'}
processManagement:
  service: {description: MongoSQL accesses MongoDB data with SQL, displayName: MongoSQL Service, name: mongosql}
schema:
  sample:
    namespaces: [kerberos.test]
security: {enabled: true}
systemLog: {verbosity: 2}
