systemLog:
  verbosity: 2
net:
  bindIp: 127.0.0.1
  unixDomainSocket:
    enabled: false
    filePermissions: "0600"
mongodb:
  net:
    uri: "mongodb://ldaptest.10gen.cc:27017"
    auth:
        mechanism: "GSSAPI"
        username: "drivers@LDAPTEST.10GEN.CC"
        password: "powerbook17"
schema:
  sample:
    namespaces: ["kerberos.test"]
security:
  enabled: true
processManagement:
  service:
    name: mongosql
    displayName: MongoSQL Service
    description: "MongoSQL accesses MongoDB data with SQL"
