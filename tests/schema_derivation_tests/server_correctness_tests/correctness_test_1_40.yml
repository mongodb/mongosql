tests:
- description: correcntess_test_1_40
  current_db: server_correctness_tests
  current_collection: fuzzer_coll
  pipeline: [ { "$sort" : { "_id" : 1 } }, { "$bucket" : { "groupBy" : { "$min" : [ "Berkshire programming Bypass", "Dynamic Investment Account Unbranded" ] }, "boundaries" : [ "Djibouti Franc", "Liberia COM", "open-source", "website Generic Concrete Tuna Cheese" ], "default" : true, "output" : { "num" : { "$avg" : { "$floor" : 62656 } } } } } ]
  result_set_schema:
    {
      "bsonType": "object",
      "properties": {
        "_id": { "bsonType": "bool" },
        "num": { "bsonType": "double" }
      },
      "required": ["_id", "num"],
      "additionalProperties": false
    }