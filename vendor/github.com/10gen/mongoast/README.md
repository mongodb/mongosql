# mongoast

mongoast holds code to represent an aggregation framework query as an abstract syntax tree. It includes parsing from bson, deparsing into bson, and a visitor implementation.

## Getting Started

Once the project is cloned, just do `go run build.go` from the root directory to list the tasks available. `go run build.go verify` should be run before pushing to master.

### Requirements

* Go 1.11
